# 盲写审计报告与修复计划

> 审计日期：2026-09-02
> 审计范围：`hardware-mall-backend` 全量 Java 源码（129 个文件）+ `hardware-mall-admin` 与 `hardware-mall-uniapp` 前端调用
> 审计方法：4 个并行探查代理交叉验证（后端内部引用一致性、前后端接口对齐、规范与逻辑隐患、DB 与代码对齐），关键高危项已人工复核

## 一、总体结论

- 跨文件引用层面（接口/实现、常量、工具类签名、实体字段、Bean 注入、事件、Mapper SQL）基本干净，**无编译级盲写断点**。
- 但存在大量典型的"单文件看是对的、放全局就错"型 bug：**高危 6 项、中危约 12 项、低危若干**。
- 高危问题集中在：订单取消竞态资损、用户封禁失效、小程序头像上传 404、管理端启停接口 400、spec_hash 协议漂移。

**当前进度**：高危 6 项中已完成 2 项（H1、H2），剩余 4 项（H3-H6）待修。中危 12 项全部待修。

## 二、审计发现

### 2.1 高危（功能必然失败 / 资损风险，已人工复核）

| # | 问题 | 位置 |
|---|------|------|
| H1 | **取消订单与支付回调竞态 → 资损**：`cancelOrder` 先查状态再无条件 `updateById` 覆盖，非 CAS。支付回调把订单推到"已付款"后，取消请求仍会把状态改回"已取消"并恢复库存，且不触发自动退款 | `OrderServiceImpl.java:317-337`（`autoCancelOrder:501-537` 同型） |
| H2 | **封禁用户形同虚设**：`login()` 不检查 `status`，被禁用户重新登录即得新 token；`refreshToken` 签发的新 token 不登记进 `user:tokens:`、不拉黑旧 token，管理员身份 claim 也丢失 | `UserServiceImpl.java:50-53,130-134` |
| H3 | **小程序头像上传 404**：调用不存在的 `/user/upload/avatar`（后端只有 `/api/admin/upload/avatar` 且有 `@RequireAdmin`），且误读响应 `data.data` 应为 `data.data.url` | `uniapp/src/pages/user/edit.vue:116,125` |
| H4 | **管理端用户启/停必 400**：前端把 `status` 放 JSON body，后端是 `@RequestParam`（对照同文件 region 接口用 `@RequestBody`，明显盲写不一致） | `admin/src/api/admin/user.ts:31-33` vs `AdminUserController.java:32` |
| H5 | **管理端物流启/停必 400**：同型问题 | `admin/src/api/admin/logistics.ts:48-50` vs `AdminLogisticsController.java:52` |
| H6 | **spec_hash 协议漂移**：seed 数据 hash 是 `'auto_c1_i1'` 人写格式，代码 `computeSpecHash` 是 MD5，永远不相等 → 唯一键 `uk_spu_specs` 防重失效，seed 商品再"生成 SKU"会落重复行 | `SkuServiceImpl.java:331-339` vs `seed_real_products_v2.sql:164` |

### 2.2 中危（静默失效 / 隐患）

| # | 问题 | 位置 |
|---|------|------|
| M1 | `BusinessException` 是死代码，所有业务错误抛裸 `RuntimeException` → 前端永远只看到"系统繁忙"，"库存不足/请勿重复下单"等全部丢失 | `GlobalExceptionHandler.java:50-55` |
| M2 | 限流是全站共享计数器（key 不含用户/IP），多人同时付款即全站 429；incr+expire 非原子 | `RateLimitInterceptor.java:35-46` |
| M3 | `test_` 登录后门无环境隔离，生产可伪造任意 openid 登录 | `WechatUtil.java:37-42,103-106` |
| M4 | SKU 增删改不失效 `product:list:*` 缓存 → 改价后列表页最长 1 小时显示旧价 | `SkuServiceImpl.java:187-233` |
| M5 | `getItemsByCategory(categoryId)` 参数被完全忽略，返回全库规格项 | `SpecItemServiceImpl.java:32-37` |
| M6 | 支付条件装配失效：`@ConditionalOnBean` 有注册顺序陷阱；`mch-id` 空串默认值使 `@ConditionalOnProperty` 恒真；`payService` 缺失时退款订单永久卡"退款中" | `PayController.java:20`、`WechatPayConfig.java:14`、`OrderServiceImpl.java:491-493` |
| M7 | `prepay`/`refund` 在事务内同步调微信 HTTP → 高并发耗尽连接池；`callback` 事务内 catch 全部异常 → `UnexpectedRollbackException` | `PayServiceImpl.java:106-183,403-437` |
| M8 | 下单幂等 key 在校验前写入且失败不回滚 → "库存不足"后用户被锁 5 分钟；key 未绑定用户 | `OrderServiceImpl.java:69-73` |
| M9 | 管理端订单搜索：`orderNo`、`startDate/endDate` 前端传了后端根本不接收 → 搜索框静默无效 | `AdminOrderController.java:21-30` |
| M10 | 发货时前端传的 `logisticsId`（物流公司）后端丢弃不入库；小程序下单 `logisticsId` 硬编码 1，物流被停用则全站无法下单 | `AdminOrderController.java:42-48`、`uniapp/checkout/index.vue:192` |
| M11 | Dashboard 趋势字段后端从不返回，页面恒显 0%（死数据） | `DashboardStatsVO.java` |
| M12 | 全部 DTO/实体入参无长度校验 → 超长直接 DB 500；`getRecentOrders` 的 `limit` 未钳制直接拼 `LIMIT ` 字符串 | `CreateOrderRequest.java:24` 等、`OrderServiceImpl.java:428` |

### 2.3 低危（本次不修复，留档）

- `user:tokens:{id}` 集合本身无 TTL（用户异常退出不调 logout 的 token 永久留在集合里）——**已缓解**：`updateUserStatus` 扫荡循环已加 per-token `catch (JwtException)`，死 token 跳过拉黑继续扫，集合整体 `delete` 一并清理；集合自身的 TTL 仍未处理（长期低危）
- `deleteSku` 不清 `sku:stock`、事务提交前写 Redis
- `PaymentRecord.getStatus()` 为 null 时 NPE（Integer == int 拆箱，多处）
- 限流/库存失败队列等 Redis key 魔法字符串游离于 `RedisConstants` 外、`PREFIX_RATE_LIMIT` 常量重复定义
- 测试 schema 缺全部 UNIQUE KEY（与生产漂移）、`payment_record` DDL 无 `IF NOT EXISTS`
- 死接口/死代码：admin 端 `/spu/category/*` 5 个、`/user/product/{id}/specs`、`admin_remark` 死列、FULLTEXT 索引无人用、`OrderItem.productId`/`cancelTime` 等前端类型声明漂移
- 退款金额 `longValue()` 应为 `longValueExact()`、`StockSyncRetryJob` 无分布式锁

## 三、修复计划

### Phase 1 — 高危 6 项

#### H1 取消订单竞态资损 ✅ 已完成（commit `a51c885`）

**目标**：`OrderServiceImpl.cancelOrder` / `autoCancelOrder`

**改动**：
- 将"查状态 → 还库存 → 无条件 updateById"改为：CAS `WHERE id=? AND status=1` 条件更新
- `affected==0` 则抛异常使整个事务回滚，与同文件 `confirmReceive` 的既有 CAS 风格对齐
- 库存恢复移到 CAS 成功之后，避免"还了库存但订单没取消"的问题
- `autoCancelOrder` 同型改造，`affected==0` 返回 false

**验证**：编译通过；OrderServiceImplTest 的 Mockito 插件初始化问题是环境配置问题，与本次修改无关

**概念**：TOCTOU（Time-of-check to time-of-use）竞态。`@Transactional` 只保证单条语句原子，不保证"检查过的条件一直成立"。`updateById` 生成的 SQL 是 `WHERE id=?`（无 status 条件），会把并发者刚写入的状态覆盖掉。修复模式：把检查焊进 SQL 的 WHERE（CAS），失败抛异常掀桌子回滚。详见附录 A。

---

#### H2 封禁失效 + refresh 链路 ✅ 已完成（commit `34cd1fa`）

**目标**：`UserServiceImpl` + `UserController` + `AdminAuthController`

**改动**：
- `login()`：已存在用户若 `status==DISABLED` 抛 `BusinessException("账号已被禁用")`
- `refreshToken(userId, oldToken)`：校验用户状态、claims 补 `role`、新 token `sAdd` 登记、旧 token `sRemove` + 拉黑（带 TTL）
- `UserController.refresh` 从 Authorization header 解出旧 token 传入
- `updateUserStatus` 加管理员守卫：`role==ADMIN` 抛 `BusinessException("管理员账号不允许封禁")`
- `AdminAuthController` **移除** admin token 登记（admin 不参与封禁登记，refresh 仅保留旧 token 拉黑）
- `updateUserStatus` 拉黑补 TTL（修复内存泄漏）

**设计决策**：封禁能力收敛为用户侧专属。管理后台用户列表里管理员行点禁用会收到明确报错，规则成为强制不变式（而非"没人调用"的约定）。

**验证**：编译通过；`UserServiceImplTest` 新增 `testUpdateUserStatus_AdminCannotBeDisabled` 用例（断言抛 `BusinessException`、不调 `updateById`、不触发 Redis 交互）

**概念**：Enforcement gap（不变式碎片化）。"被封禁的用户不能持有任何有效凭证"是全局规则，但四个发证口（user login / user refresh / admin login / admin refresh）里只有一个登记在册、没有一个查禁用状态。修复模式：在每个发证口补齐同一套动作（登记 + 校验 + 轮换），或收敛到 service 唯一一处。详见附录 B。

**H2 补丁 — 扫荡循环加固**（未提交）：

**问题**：`updateUserStatus` 的扫荡循环对 `user:tokens:{id}` 集合里每个 token 调 `jwtUtil.getExpirationFromToken(token)`，过期 token 会抛 `ExpiredJwtException` → 整个禁用请求失败（"系统繁忙"）。由于 `userMapper.updateById(user)` 在扫荡之前已自动提交，数据库实际已禁用，但前端报错、活 token 未拉黑、集合未清理（重试还炸）。

**根因**：`user:tokens:{id}` 集合无 TTL，用户异常退出（不调 logout）的死 token 永久残留。低危清单当时判断"无害但集合无限增长"，低估了——扫荡会因死 token 崩溃。

**改动**：`updateUserStatus` 扫荡循环体加 `try-catch (JwtException)`，死 token 跳过拉黑继续扫，集合整体 `delete` 一并清理。新增测试 `testUpdateUserStatus_DisableSweepSkipsExpiredTokens` 验证混合 token 场景。

---

#### H3 小程序头像上传 404 ✅ 已完成

**目标**：新增 `controller/user/UserUploadController` + 前端 `edit.vue`

**改动**：
- 新增 `POST /api/user/upload/avatar`（登录态、限图片、复用 OssService，路径 `avatars/`）
- 校验链：非空 → 类型白名单(jpg/jpeg/png/webp) + contentType 前缀 image/ → 大小 ≤2MB
- `edit.vue:125` 改读 `data.data.url`

**验证**：编译通过；运行时验证（需启动服务后手动 curl）：带 token → 200 且 data.url 可访问；无 token → 401

---

#### H4/H5 启停接口 400 ✅ 已完成

**目标**：`AdminUserController.updateUserStatus`、`AdminLogisticsController.updateStatus`

**改动**：
- `@RequestParam` → `@RequestBody Map<String,Integer>`（与同文件 region 接口风格一致）
- `AdminLogisticsController` 补 `import java.util.Map;`

**验证**：编译通过；前端启停操作测试

---

#### H6 spec_hash 漂移

**目标**：`SkuServiceImpl`

**改动**：
- 规格去重不再依赖存储的 hash 字符串相等：改用 specs 归一化 key（排序后 `templateId:itemId` 多重集，`templateId==null` 记 `"default"`）做匹配，兼容 seed 的 `auto_c*_i*` 与 MD5 两种存量 hash
- 顺带消除 null templateId 排序 NPE
- `createSku` 保存时若归一化 key 已存在则跳过/报错
- 更新 `SkuServiceLogicTest`

**验证**：`SkuServiceLogicTest` 补 seed 兼容场景

---

### Phase 2 — 中危 12 项

| 项 | 改动 |
|---|---|
| M1 | 全部业务 `RuntimeException(业务msg)` → `BusinessException`（grep 全量替换，全局处理器已支持） |
| M2 | `RateLimitInterceptor`：key 追加 `userId`（登录态）/客户端 IP（匿名）；incr+expire 改 Lua 原子执行 |
| M3 | `WechatUtil` 的 `test_` 后门加 `wechat.mock-code-enabled` 开关，默认 false，dev yml 置 true |
| M4 | `SkuServiceImpl` 增删改补 `deleteByPattern(PREFIX_PRODUCT_LIST + "*")`；顺带 `deleteSku` 清 `sku:stock` |
| M5 | `getItemsByCategory`：按 categoryId 查模板 id 集合再 `in(templateId)` 过滤（复用现有 line:85 模式） |
| M6 | `WechatPayConfig` 改 `@ConditionalOnProperty(name="wechat.pay.enabled", havingValue="true")` + yml 显式开关；`PayController` 改 `ObjectProvider<PayService>` 风格；`refundOrder` 中 `payService==null` 时抛异常回滚 CAS，订单不再卡"退款中" |
| M7 | `prepay`/`refund` 拆出事务外微信调用（TransactionTemplate 分段）；`callback` 事务注解下沉到 `processPaymentSuccess`（经代理调用），catch 移到事务外，消除 `UnexpectedRollbackException` |
| M8 | 幂等 key：冲突时校验 value 归属；`createOrder` 失败路径删除幂等 key 后重新抛出 |
| M9 | `AdminOrderController.getOrderList` 补 `orderNo`/`startDate`/`endDate` 参数，`getAdminOrderPage` 补过滤（orderNo 精确、日期按 createTime 区间），更新接口签名 |
| M10 | `shipOrder` 链路补 `logisticsId`：校验存在且启用后写入订单；uniapp `checkout` 改为调用物流列表接口传真实 id（一处小前端改动） |
| M11 | `DashboardStatsVO` 补 trend 四字段：今日 vs 昨日订单数/销售额/发货量/上新量涨幅，补 mapper 查询 |
| M12 | 关键入参补 `@Size`（buyerRemark/detail/consignee/nickname/name 等）+ `@Valid`；`getRecentOrders` 的 limit 用 `PageUtil.clampLimit` |

## 四、验证策略

- 每阶段结束 `mvn compile`（AGENTS.md 规则）
- 跑受影响单测：`OrderServiceImplTest`、`SkuServiceLogicTest`、`SkuServiceImplTest`、`UserServiceImplTest`、`PayServiceImplTest`，签名变更处同步更新测试
- M7 改造为 `callback` 补一个异常回滚场景的单元测试

## 五、涉及文件清单

### 后端（约 22 个）

- `OrderServiceImpl`、`OrderService`（接口）
- `UserServiceImpl`、`UserService`（接口）
- `UserController`、`AdminAuthController`
- `SkuServiceImpl`
- `SpecItemServiceImpl`
- `RateLimitInterceptor`
- `WechatUtil`
- `WechatPayConfig`、`PayController`、`PayServiceImpl`
- `AdminUserController`、`AdminLogisticsController`、`AdminOrderController`
- `DashboardController`、`DashboardStatsVO`
- `CreateOrderRequest`
- `GlobalExceptionHandler`（核对）
- 新增：`UserUploadController`

### 前端（2 个）

- `uniapp/src/pages/user/edit.vue`
- `uniapp/src/pages/checkout/index.vue`

## 六、决策记录

| 决策点 | 选择 | 理由 |
|---|---|---|
| spec_hash 漂移（H6） | 改代码兼容 seed | 现有数据库数据无需迁移，风险最小 |
| 订单搜索/发货物流补齐（M9/M10） | 改后端补齐 | 前端已有 UI，后端缺参数 |
| 修复范围 | 高危 + 中危 | 低危留档后续清理 |
| H2 admin 封禁 | 删除 admin 封禁功能 | admin 不需要封禁，只有 user 需要；封禁能力收敛为用户侧专属 |
| H2 admin refresh 旧 token | 保留拉黑 | 防刷新后旧 token 继续可用，不依赖封禁登记，成本低 |
| H2 异常类型 | 顺手改 BusinessException | "账号已被禁用"报错文案直达前端，不等 M1 统一治理 |

## 七、概念附录

### A. CAS（Compare-And-Swap）与 TOCTOU

**问题**：`check-then-act` 模式（先查状态再改状态）在并发下会被人插队。`@Transactional` 只保证单条语句原子，不保证"检查过的条件一直成立"。

**修复**：把检查焊进 SQL 的 WHERE（CAS），失败抛异常掀桌子回滚。

**项目里的例子**：
- ✅ `confirmReceive`（3→4）、`refundOrder`（2/3→6）、`processPaymentSuccess`（1→2）都用了 CAS
- ❌ `cancelOrder`（1→5）漏了 → H1 修复

**类比**：抢票时你看了眼座位表（A 区 3 排还有座），去付款的路上别人坐下了，你回来直接把名字写上去，还把人家涂了。`updateById` 就像 `git push --force` 旧本地副本；CAS 是 `git push` 带 rebase 检查。

### B. Enforcement gap（不变式碎片化）

**问题**：全局规则（如"被封禁的用户不能持有任何有效凭证"）横跨多套状态（DB `user.status` / JWT+Redis 凭证集），但只在部分口子强制执行。每个方法局部看都"职责单一、逻辑正确"，拼起来不变式就漏风。

**修复**：在每个发证口补齐同一套动作（登记 + 校验 + 轮换），或收敛到 service 唯一一处。

**项目里的例子**：
- ✅ `UserController.login:37` 有 `sAdd` 登记
- ❌ `refreshToken`、`AdminAuthController.login`、`AdminAuthController.refresh` 不登记 → H2 修复

**类比**：开除员工 = 收走门禁卡（拉黑登记集），但这家公司：门禁只认"报废卡清单"、不认花名册（拦截器不查 status）；员工可以自助复制卡且复制卡不用报备（refresh 不登记）；补办柜台不查离职名单（login 不查 status）。收卡收得再勤快也没用。

### C. 掀桌子 vs 补偿（两种失败处理流派）

**分界线**：失败后能不能"当作没发生"？

| 流派 | 适用场景 | 项目里的例子 |
|---|---|---|
| **掀桌子**（rollback） | 无外部不可逆动作，失败即全回滚 | `cancelOrder`、`confirmReceive`、`refundOrder` 里 `payService==null` 时抛异常回滚 CAS |
| **补偿**（compensation） | 外部不可逆动作已发生（钱已扣/货已发/短信已发） | `processPaymentSuccess` 的 ④ 自动退款（钱已扣，订单已取消，补偿退款） |

**关键**：事务能回滚数据库，回滚不了世界。`refundService.create()`（`PayServiceImpl:419`）是 HTTPS 请求，微信已受理即不可逆。所以 `refundOrder` 用 claim-first（先 CAS 占位 6，事务外调微信），失败时保持 6 等回调自愈。

### D. 事务内操作顺序的防御性布局

**原则**：把裁决（CAS + 失败抛异常）放在事务的最后一步。裁决通过 = 终局，此后没有任何可能失败的写操作；裁决失败 = 掀桌子，此前做的一切全部作废。

**对比**：
- ✅ `cancelOrder`（H1 修复后）：还库存 → CAS 状态 → 失败抛异常回滚。裁决在最后，失败方向干净（什么都没发生）
- ❌ 假设反过来：CAS 状态 → 还库存。裁决通过后还留着一个可失败的步骤，若还库存失败被 catch 吞掉 → 提交 → "已取消但库存没还"永久落库

**例外**：`processPaymentSuccess` 的"裁决不在最后"是对的，因为它是补偿流派——钱已扣（外部不可逆），必须记录事实（payment_record=PAID）然后补偿（退款）。这里的"半完成状态"是故意的且可见的，立即有补偿在同一个事务里，补偿失败也有钉钉告警兜底。

### E. 盲写的三种形态

1. **脑补契约**：前端想象了一个后端没有的接口（H3 头像上传 404）。判断技巧：凡是"前端用了后端不存在的路径"，基本可以断定那一段前端是从需求脑补的，而不是对着 controller 写的。
2. **局部自洽但全局漏**：每个方法职责都自洽，缺的是跨方法的约定（H2 封禁失效、H1 取消竞态）。
3. **不变式碎片化**：全局规则只在部分口子强制执行（H2 的四个发证口只有一个登记）。

## 八、待办清单

### Phase 1 — 高危剩余 4 项

- [ ] H3 小程序头像上传 404
- [ ] H4 管理端用户启/停必 400
- [ ] H5 管理端物流启/停必 400
- [ ] H6 spec_hash 协议漂移

### Phase 2 — 中危 12 项

（见第三节 Phase 2 表格）
