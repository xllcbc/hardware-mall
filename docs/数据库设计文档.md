# 五金商城系统数据库设计文档

**文档版本**：V2.0
**撰写日期**：2026年3月28日
**最后更新**：2026年4月25日
**项目名称**：五金商城系统（锁具+胶类+当地专线物流园）
**数据库类型**：MySQL 8.0

---

## 一、文档概述

### 1.1 编写目的

本文档旨在详细描述五金商城系统的数据库设计，为后端开发人员提供完整的数据库结构参考。文档涵盖了系统的所有数据表结构设计、字段定义、数据类型选择、索引设计、外键关系以及业务规则说明。

### 1.2 技术选型说明

- **数据库**：MySQL 8.0
- **字符集**：utf8mb4
- **排序规则**：utf8mb4_general_ci
- **ORM框架**：MyBatis-Plus

### 1.3 核心设计变化（V2.0）

项目从 V1.0 的简单商品模型演进为 **SPU + SKU + 规格模板** 模型：

- **SPU（商品型号）**：商品的公共信息，如名称、副标题、描述、图片等
- **SKU（具体规格）**：SPU 下的具体规格组合，如"金色/50mm"，包含价格和库存
- **规格模板（SpecTemplate）**：分类下定义的规格类型，如"颜色"、"锁体大小"
- **规格项（SpecItem）**：规格模板的可选值，如"金色"、"银色"、"50mm"

---

## 二、实体关系设计

### 2.1 实体关系图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   user      │       │   cart      │       │    spu      │
│─────────────│       │─────────────│       │─────────────│
│ id (PK)     │───┐   │ id (PK)     │       │ id (PK)     │
│ openid      │   │   │ user_id(FK) │───────┤ category_id │
│ nickname    │   └───│ sku_id(FK)  │       │ name        │
│ avatar_url  │       │ quantity    │       │ subtitle    │
│ phone       │       └─────────────┘       │ images      │
│ role        │                             │ status      │
│ status      │       ┌─────────────┐       │ is_recommend│
│ create_time │       │   address   │       └──────┬──────┘
└─────────────┘       │─────────────│              │
                      │ id (PK)     │              │
                      │ user_id(FK) │              │
                      │ consignee   │              │
                      │ phone       │              │
                      │ province    │              │
                      │ city        │              │
                      │ district    │              │
                      │ detail      │              │
                      └─────────────┘              │
                                                 │
                      ┌─────────────┐       ┌──────┴──────┐
                      │  shop_order │       │  category   │
                      │─────────────│       │────────────│
┌─────────────────────┤ id (PK)     │       │ id (PK)    │
│                     │ order_no    │       │ name       │
│                     │ user_id(FK) │───────┤ icon       │
│                     │ address_id  │       │ sort_order  │
│                     │ logistics_id│       │ status     │
│                     │ status      │       └─────────────┘
│                     │ total_amount│
│                     │ pay_amount  │       ┌─────────────┐
│                     └──────┬──────┘       │    sku     │
│                            │              │─────────────│
│                            │              │ id (PK)     │
│                     ┌──────▼──────┐       │ spu_id (FK)│
│                     │ order_item  │       │ specs (JSON)
│                     │──────────────│       │ price      │
│                     │ id (PK)     │       │ stock      │
│                     │ order_id(FK)│───────┤ image      │
│                     │ sku_id (FK) │       │ status     │
│                     │ spu_id (FK) │       │ spec_hash  │
│                     │ product_name│       └─────────────┘
│                     │ product_spec│              │
│                     │ price       │              │
│                     │ quantity    │              │
│                     └──────────────┘              │
                                                 │
                      ┌─────────────┐       ┌──────┴──────┐
                      │  logistics  │       │spec_template
                      │─────────────│       │────────────│
                      │ id (PK)     │       │ id (PK)    │
                      │ name        │       │ category_id│
                      │ code        │       │ name       │
                      │ contact     │       │ spec_type  │
                      │ phones (JSON)      │ is_required │
                      │ city        │       │ sort_order  │
                      │ address     │       └──────┬──────┘
                      │ sort_order  │             │
                      │ status      │       ┌──────▼──────┐
                      └─────────────┘       │ spec_item  │
                                          │────────────│
                                          │ id (PK)    │
                                          │ template_id│
                                          │ value      │
                                          │ sort_order │
                                          └────────────┘
```

### 2.2 实体关系说明

- **user ↔ address**: 一对多，用户可拥有多个收货地址
- **user ↔ cart**: 一对多，购物车以 SKU 为单位
- **user ↔ shop_order**: 一对多，用户可创建多个订单
- **category ↔ spu**: 一对多，分类下可有多个商品型号
- **spu ↔ sku**: 一对多，一个 SPU 包含多个 SKU 规格组合
- **category ↔ spec_template**: 一对多，分类下定义多个规格模板
- **spec_template ↔ spec_item**: 一对多，规格模板包含多个规格项
- **shop_order ↔ order_item**: 一对多，订单包含多个订单明细
- **order_item → sku/spu**: 订单明细保存 SKU 和 SPU 快照

---

## 三、表结构详细设计

### 3.1 用户表（user）

```sql
CREATE TABLE `user` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '用户ID',
    `openid` VARCHAR(64) NOT NULL COMMENT '微信OpenID',
    `unionid` VARCHAR(64) DEFAULT NULL COMMENT '微信UnionID',
    `nickname` VARCHAR(50) DEFAULT NULL COMMENT '用户昵称',
    `avatar_url` VARCHAR(255) DEFAULT NULL COMMENT '用户头像URL',
    `phone` VARCHAR(20) DEFAULT NULL COMMENT '用户手机号',
    `province` VARCHAR(50) DEFAULT NULL COMMENT '用户所在省份',
    `city` VARCHAR(50) DEFAULT NULL COMMENT '用户所在城市',
    `role` TINYINT NOT NULL DEFAULT 1 COMMENT '用户角色：1-普通用户，2-管理员',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '用户状态：1-正常，0-禁用',
    `last_login_time` DATETIME DEFAULT NULL COMMENT '最后登录时间',
    `last_login_ip` VARCHAR(50) DEFAULT NULL COMMENT '最后登录IP地址',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_openid` (`openid`),
    UNIQUE KEY `uk_phone` (`phone`),
    KEY `idx_status` (`status`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='用户表';
```

### 3.2 收货地址表（address）

```sql
CREATE TABLE `address` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '地址ID',
    `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    `consignee` VARCHAR(50) NOT NULL COMMENT '收货人姓名',
    `phone` VARCHAR(20) NOT NULL COMMENT '收货人手机号',
    `province` VARCHAR(50) NOT NULL COMMENT '省份',
    `city` VARCHAR(50) NOT NULL COMMENT '城市',
    `district` VARCHAR(50) NOT NULL COMMENT '区县',
    `detail` VARCHAR(255) NOT NULL COMMENT '详细地址',
    `postal_code` VARCHAR(10) DEFAULT NULL COMMENT '邮政编码',
    `is_default` TINYINT NOT NULL DEFAULT 0 COMMENT '是否默认地址：1-默认，0-非默认',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_user_default` (`user_id`, `is_default`),
    CONSTRAINT `fk_address_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='收货地址表';
```

### 3.3 商品分类表（category）

```sql
CREATE TABLE `category` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '分类ID',
    `parent_id` BIGINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '父级分类ID，0表示顶级分类',
    `name` VARCHAR(50) NOT NULL COMMENT '分类名称',
    `icon` VARCHAR(255) DEFAULT NULL COMMENT '分类图标URL或emoji',
    `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序权重，数值越大越靠前',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '分类状态：1-启用，0-禁用',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    KEY `idx_parent_id` (`parent_id`),
    KEY `idx_status_sort` (`status`, `sort_order` DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='商品分类表';
```

### 3.4 SPU商品型号表（spu）

SPU 是商品的公共信息载体，存储商品名称、副标题、描述、图片等公共属性。

```sql
CREATE TABLE `spu` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '商品型号ID',
    `category_id` BIGINT UNSIGNED NOT NULL COMMENT '分类ID',
    `name` VARCHAR(100) NOT NULL COMMENT '商品名称(SPU)',
    `subtitle` VARCHAR(200) DEFAULT NULL COMMENT '商品副标题',
    `description` TEXT DEFAULT NULL COMMENT '商品详细描述，支持富文本',
    `images` JSON DEFAULT NULL COMMENT '商品图片列表，JSON数组格式',
    `original_price` DECIMAL(10,2) DEFAULT NULL COMMENT '商品原价',
    `weight` DECIMAL(10,2) DEFAULT NULL COMMENT '商品重量，单位：千克',
    `sales_count` INT NOT NULL DEFAULT 0 COMMENT '商品销量',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '商品状态：1-上架，0-下架',
    `is_recommend` TINYINT NOT NULL DEFAULT 0 COMMENT '是否推荐商品：1-推荐，0-非推荐',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    KEY `idx_category_id` (`category_id`),
    KEY `idx_status` (`status`),
    KEY `idx_status_recommend` (`status`, `is_recommend`),
    KEY `idx_status_sales` (`status`, `sales_count` DESC),
    KEY `idx_name` (`name`),
    FULLTEXT KEY `ft_name` (`name`),
    CONSTRAINT `fk_spu_category` FOREIGN KEY (`category_id`) REFERENCES `category` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='商品型号表(SPU)';
```

**说明**：
- `images`: JSON数组存储多张商品图片
- `original_price`: 原价，用于显示折扣
- `sales_count`: 销量，下单完成后更新
- 价格和库存不在 SPU 层，而在 SKU 层

### 3.5 SKU具体规格表（sku）

SKU 是 SPU 下的具体规格组合，每种组合有独立的价格和库存。

```sql
CREATE TABLE `sku` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT 'SKU ID',
    `spu_id` BIGINT UNSIGNED NOT NULL COMMENT 'SPU ID',
    `specs` JSON NOT NULL COMMENT '规格组合 [{"templateId":1,"itemId":1,"name":"颜色","value":"金色"},...]',
    `price` DECIMAL(10,2) NOT NULL DEFAULT 0 COMMENT 'SKU价格',
    `stock` INT NOT NULL DEFAULT 0 COMMENT 'SKU库存数量',
    `image` VARCHAR(255) DEFAULT NULL COMMENT 'SKU图片',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT 'SKU状态：1-启用，0-禁用',
    `spec_hash` VARCHAR(64) NOT NULL COMMENT '规格组合哈希值，用于快速查找',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_spu_specs` (`spu_id`, `spec_hash`) COMMENT '同一SPU下规格组合唯一',
    KEY `idx_spu_id` (`spu_id`),
    KEY `idx_status` (`status`),
    KEY `idx_stock` (`stock`),
    CONSTRAINT `fk_sku_spu` FOREIGN KEY (`spu_id`) REFERENCES `spu` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='商品SKU表';
```

**说明**：
- `specs`: JSON数组存储规格组合，如 `[{"templateId":1,"itemId":1,"name":"颜色","value":"金色"},{"templateId":2,"itemId":4,"name":"锁体大小","value":"50mm"}]`
- `spec_hash`: 规格组合的哈希值，用于唯一性约束和快速查找
- `price`: SKU级别的价格
- `stock`: SKU级别的库存

### 3.6 规格模板表（spec_template）

规格模板定义分类下有哪些规格类型，如"颜色"、"尺寸"等。

```sql
CREATE TABLE `spec_template` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '规格模板ID',
    `category_id` BIGINT UNSIGNED NOT NULL COMMENT '分类ID',
    `name` VARCHAR(50) NOT NULL COMMENT '规格名称(如:颜色/尺寸/锁体大小)',
    `spec_type` TINYINT NOT NULL DEFAULT 1 COMMENT '规格类型:1-选择型,2-输入型',
    `is_required` TINYINT NOT NULL DEFAULT 1 COMMENT '是否必选:1-必选,0-可选',
    `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序权重',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    KEY `idx_category_id` (`category_id`),
    KEY `idx_category_sort` (`category_id`, `sort_order`),
    CONSTRAINT `fk_spec_template_category` FOREIGN KEY (`category_id`) REFERENCES `category` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='规格模板表';
```

**说明**：
- `spec_type`: 1-选择型（从预设值选择），2-输入型（用户手动输入）
- `is_required`: 是否必填规格

### 3.7 规格项表（spec_item）

规格项是规格模板的可选值集合。

```sql
CREATE TABLE `spec_item` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '规格项ID',
    `template_id` BIGINT UNSIGNED NOT NULL COMMENT '所属模板ID',
    `value` VARCHAR(50) NOT NULL COMMENT '规格值(如:白色/50mm/金色)',
    `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序权重',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    KEY `idx_template_id` (`template_id`),
    KEY `idx_template_sort` (`template_id`, `sort_order`),
    CONSTRAINT `fk_spec_item_template` FOREIGN KEY (`template_id`) REFERENCES `spec_template` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='规格项表';
```

### 3.8 购物车表（cart）

购物车以 SKU 为单位存储。

```sql
CREATE TABLE `cart` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '购物车ID',
    `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    `sku_id` BIGINT UNSIGNED NOT NULL COMMENT 'SKU ID',
    `quantity` INT UNSIGNED NOT NULL DEFAULT 1 COMMENT '购买数量',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_sku` (`user_id`, `sku_id`) COMMENT '同一用户同一SKU只允许一条记录',
    KEY `idx_user_id` (`user_id`),
    KEY `idx_sku_id` (`sku_id`),
    CONSTRAINT `fk_cart_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE,
    CONSTRAINT `fk_cart_sku` FOREIGN KEY (`sku_id`) REFERENCES `sku` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='购物车表';
```

### 3.9 物流方式表（logistics）

```sql
CREATE TABLE `logistics` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '物流ID',
    `name` VARCHAR(50) NOT NULL COMMENT '物流名称',
    `code` VARCHAR(50) NOT NULL COMMENT '物流编码',
    `description` VARCHAR(255) DEFAULT NULL COMMENT '物流描述/备注',
    `contact` VARCHAR(50) DEFAULT NULL COMMENT '物流联系人',
    `phones` JSON DEFAULT NULL COMMENT '物流联系电话列表',
    `city` VARCHAR(50) DEFAULT NULL COMMENT '物流所在城市',
    `address` VARCHAR(255) DEFAULT NULL COMMENT '物流网点地址',
    `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序权重',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '物流状态：1-启用，0-禁用',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `delete_time` BIGINT DEFAULT 0 COMMENT '删除时间，软删除标识',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_code` (`code`),
    KEY `idx_status_sort` (`status`, `sort_order` DESC),
    KEY `idx_city` (`city`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='物流方式表';
```

### 3.10 订单主表（shop_order）

```sql
CREATE TABLE `shop_order` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '订单ID',
    `order_no` VARCHAR(32) NOT NULL COMMENT '订单编号',
    `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    `address_id` BIGINT UNSIGNED NOT NULL COMMENT '收货地址ID',
    `logistics_id` BIGINT UNSIGNED NOT NULL COMMENT '物流方式ID',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '订单状态：1-待付款，2-待发货，3-已发货，4-已完成，5-已取消，6-退款中，7-已退款',
    `total_amount` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '订单总金额',
    `freight_amount` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '运费金额',
    `pay_amount` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '实付金额',
    `logistics_no` VARCHAR(50) DEFAULT NULL COMMENT '物流单号',
    `pay_time` DATETIME DEFAULT NULL COMMENT '支付时间',
    `ship_time` DATETIME DEFAULT NULL COMMENT '发货时间',
    `receive_time` DATETIME DEFAULT NULL COMMENT '收货时间',
    `cancel_time` DATETIME DEFAULT NULL COMMENT '取消时间',
    `cancel_reason` VARCHAR(255) DEFAULT NULL COMMENT '取消原因',
    `buyer_remark` VARCHAR(500) DEFAULT NULL COMMENT '买家备注',
    `admin_remark` VARCHAR(500) DEFAULT NULL COMMENT '管理员备注',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `admin_delete_time` BIGINT DEFAULT NULL COMMENT '管理员删除时间',
    `user_delete_time` BIGINT DEFAULT NULL COMMENT '用户删除时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_order_no` (`order_no`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_status` (`status`),
    KEY `idx_create_time` (`create_time`),
    KEY `idx_pay_time` (`pay_time`),
    KEY `idx_user_status` (`user_id`, `status`),
    KEY `idx_status_create` (`status`, `create_time` DESC),
    CONSTRAINT `fk_order_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`),
    CONSTRAINT `fk_order_address` FOREIGN KEY (`address_id`) REFERENCES `address` (`id`),
    CONSTRAINT `fk_order_logistics` FOREIGN KEY (`logistics_id`) REFERENCES `logistics` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='订单主表';
```

**说明**：
- 双向软删除：`admin_delete_time`（管理员删除）和 `user_delete_time`（用户删除）
- 用户看到的订单列表按 `user_delete_time` 过滤
- 管理端看到的订单列表按 `admin_delete_time` 过滤

### 3.11 订单明细表（order_item）

```sql
CREATE TABLE `order_item` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '订单明细ID',
    `order_id` BIGINT UNSIGNED NOT NULL COMMENT '订单ID',
    `sku_id` BIGINT UNSIGNED NOT NULL COMMENT 'SKU ID(下单时快照)',
    `spu_id` BIGINT UNSIGNED NOT NULL COMMENT 'SPU ID(下单时快照)',
    `product_name` VARCHAR(100) NOT NULL COMMENT '商品名称快照',
    `product_spec` VARCHAR(500) DEFAULT NULL COMMENT '商品规格快照(JSON)',
    `product_image` VARCHAR(255) DEFAULT NULL COMMENT '商品图片快照',
    `price` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '商品单价快照',
    `quantity` INT UNSIGNED NOT NULL DEFAULT 1 COMMENT '购买数量',
    `subtotal` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '小计金额',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_order_id` (`order_id`),
    KEY `idx_sku_id` (`sku_id`),
    CONSTRAINT `fk_order_item_order` FOREIGN KEY (`order_id`) REFERENCES `shop_order` (`id`) ON DELETE CASCADE,
    CONSTRAINT `fk_order_item_sku` FOREIGN KEY (`sku_id`) REFERENCES `sku` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='订单明细表';
```

**说明**：
- 保存下单时刻的 SKU 和 SPU 快照信息
- `product_spec`: JSON格式存储规格组合快照

---

## 四、索引设计说明

### 4.1 索引清单

| 序号 | 表名 | 索引名称 | 类型 | 字段 | 用途 |
|------|------|----------|------|------|------|
| 1 | user | PRIMARY | 主键 | id | 主键 |
| 2 | user | uk_openid | 唯一 | openid | 微信登录 |
| 3 | user | uk_phone | 唯一 | phone | 手机号唯一 |
| 4 | user | idx_status | 普通 | status | 状态筛选 |
| 5 | user | idx_create_time | 普通 | create_time | 时间排序 |
| 6 | address | PRIMARY | 主键 | id | 主键 |
| 7 | address | idx_user_id | 普通 | user_id | 用户地址查询 |
| 8 | address | idx_user_default | 复合 | user_id, is_default | 默认地址 |
| 9 | category | PRIMARY | 主键 | id | 主键 |
| 10 | category | idx_parent_id | 普通 | parent_id | 子分类查询 |
| 11 | category | idx_status_sort | 复合 | status, sort_order | 分类列表 |
| 12 | spu | PRIMARY | 主键 | id | 主键 |
| 13 | spu | idx_category_id | 普通 | category_id | 分类商品 |
| 14 | spu | idx_status | 普通 | status | 上下架筛选 |
| 15 | spu | idx_status_recommend | 复合 | status, is_recommend | 推荐商品 |
| 16 | spu | idx_status_sales | 复合 | status, sales_count | 热销商品 |
| 17 | spu | idx_name | 普通 | name | 名称筛选 |
| 18 | spu | ft_name | 全文 | name | 名称搜索 |
| 19 | sku | PRIMARY | 主键 | id | 主键 |
| 20 | sku | uk_spu_specs | 唯一 | spu_id, spec_hash | 规格唯一 |
| 21 | sku | idx_spu_id | 普通 | spu_id | SPU的SKU列表 |
| 22 | sku | idx_status | 普通 | status | 状态筛选 |
| 23 | sku | idx_stock | 普通 | stock | 库存查询 |
| 24 | spec_template | PRIMARY | 主键 | id | 主键 |
| 25 | spec_template | idx_category_id | 普通 | category_id | 分类规格 |
| 26 | spec_template | idx_category_sort | 复合 | category_id, sort_order | 规格列表 |
| 27 | spec_item | PRIMARY | 主键 | id | 主键 |
| 28 | spec_item | idx_template_id | 普通 | template_id | 模板规格项 |
| 29 | spec_item | idx_template_sort | 复合 | template_id, sort_order | 规格项排序 |
| 30 | cart | PRIMARY | 主键 | id | 主键 |
| 31 | cart | uk_user_sku | 唯一 | user_id, sku_id | 去重 |
| 32 | cart | idx_user_id | 普通 | user_id | 用户购物车 |
| 33 | cart | idx_sku_id | 普通 | sku_id | SKU关联 |
| 34 | logistics | PRIMARY | 主键 | id | 主键 |
| 35 | logistics | uk_code | 唯一 | code | 编码唯一 |
| 36 | logistics | idx_status_sort | 复合 | status, sort_order | 物流列表 |
| 37 | logistics | idx_city | 普通 | city | 城市筛选 |
| 38 | shop_order | PRIMARY | 主键 | id | 主键 |
| 39 | shop_order | uk_order_no | 唯一 | order_no | 订单号唯一 |
| 40 | shop_order | idx_user_id | 普通 | user_id | 用户订单 |
| 41 | shop_order | idx_status | 普通 | status | 状态筛选 |
| 42 | shop_order | idx_create_time | 普通 | create_time | 时间排序 |
| 43 | shop_order | idx_pay_time | 普通 | pay_time | 支付统计 |
| 44 | shop_order | idx_user_status | 复合 | user_id, status | 用户订单筛选 |
| 45 | shop_order | idx_status_create | 复合 | status, create_time | 订单列表 |
| 46 | order_item | PRIMARY | 主键 | id | 主键 |
| 47 | order_item | idx_order_id | 普通 | order_id | 订单明细 |
| 48 | order_item | idx_sku_id | 普通 | sku_id | SKU统计 |

---

## 五、业务规则说明

### 5.1 SPU/SKU 关系规则

1. 一个 SPU 必须至少有一个 SKU
2. 同一 SPU 下，规格组合（spec_hash）必须唯一
3. SKU 的 `specs` JSON 中每个规格项必须属于该分类的规格模板
4. 删除 SPU 时，其下所有 SKU 会被级联删除
5. 商品详情页显示 SPU 信息 + 所有 SKU 列表 + 规格选择器

### 5.2 规格模板规则

1. 分类可以定义多个规格模板
2. 规格模板分为选择型（spec_type=1）和输入型（spec_type=2）
3. 必选规格（is_required=1）用户在购买时必须选择/填写
4. 删除规格模板时，其下所有规格项会被级联删除

### 5.3 购物车规则

1. 购物车以 SKU 为单位，而非 SPU
2. 同一用户对同一 SKU 只能有一条记录，修改数量时更新该记录
3. 数量范围：1-99
4. 商品下架后，购物车记录保留，前端提示用户

### 5.4 订单规则

1. 创建订单时保存商品快照（sku_id, spu_id, product_name, product_spec, price）
2. 订单支持双向软删除：管理员删除和用户删除分开处理
3. 用户取消订单仅限「待付款」状态
4. 退款仅限「待发货」和「已发货」状态

### 5.5 价格计算规则

1. 订单金额单位为元（DECIMAL），保留两位小数
2. 金额计算在后端完成，前端仅做展示
3. 订单明细小计 = SKU价格 × 数量
4. 订单总金额 = 所有明细小计之和 + 运费

---

## 六、附录

### 6.1 数据库初始化

项目使用 `init.sql` + `init_spec.sql` 两个脚本：

1. **init.sql**: 基础表结构（user, address, category, logistics, shop_order, order_item）
2. **init_spec.sql**: SPU/SKU 相关表（spu, sku, spec_template, spec_item）和购物车改造

---

**文档状态**：已更新至 V2.0
**更新内容**：SPU+SKU+规格模板模型重构
